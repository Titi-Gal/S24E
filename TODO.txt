

Select Bridge do build twice screen:
    make select bridged to build by select each remaining vertice when there are more then 20 vertices
        make so there id not adjacent vertice in next choices
        validate that buildingRepeatedDiagonals.length == (numberOfVertices - 2) / 2
    